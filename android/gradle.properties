org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Mixed Kotlin Gradle Plugin versions left incomplete release class jars for
# device_info_plus / package_info_plus. Disable incremental Kotlin so a clean
# compile always emits the plugin classes GeneratedPluginRegistrant needs.
kotlin.incremental=false
# Do not set org.gradle.java.home here — that path is machine-specific and
# breaks other machines (e.g. /opt/homebrew/opt/openjdk@21 does not exist).
# If Android Studio's JBR is too new for Kotlin, pin Java in
# ~/.gradle/gradle.properties instead, for example:
# org.gradle.java.home=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
